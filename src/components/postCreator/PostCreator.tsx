/**
 * Contains a simple textbox that allows people to make a post.
 * The optional `clusterID` prop allows you to post to a specific cluster.
 */
export default function PostCreator({clusterID}: {clusterID?: string}) {}
