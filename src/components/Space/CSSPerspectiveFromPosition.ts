import { SpacePositionInfo } from '../../typings/SpaceParticipant';

/**
 * Creates a perspective string that can be used in the parent component of a Space map
 *
 * @param position The position
 */
export default function CSSPerspectiveFromPosition(position: SpacePositionInfo) {}
